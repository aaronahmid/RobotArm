from pydantic_settings import BaseSettings

class Settings(BaseSettings):
    PROJECT_NAME: str = "{{ .Name }}"
    VERSION: str = "{{ .Version }}"
    
    class Config:
        env_file = ".env"

settings = Settings()

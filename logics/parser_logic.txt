
- check args length:
    if length < 2:
        print usage, exit

- extract arg from args

- check if arg is an option:
    if arg is option:
        pass
    else:
        print usage, exit

if args_length == 2:
    if arg not version[-v/--version] or help[-h/--help] option:
        print option usage, exit
    else:
        print version or help manual, exit
else if args_length > 2:
    - store args in args_list, pop args_list[0]
    call the appropiate arg controller
        pass in args_list



